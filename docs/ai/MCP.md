# 표준화된 도구 통합 및 외부 API 활용 설계 

## MCP 사용 여부
'맛침반' 프로젝트에서는 외부 상용 API를 사용하지 않고 검색, 리랭킹, 생성 모듈이 내부 서비스로 분리되어 있어 MCP 도입의 이점이 제한적이다.

또한 RAG 파이프라인의 핵심은 검색 정확도와 규칙 기반 보정 로직이므로 표준 프로토콜보다 직접 제어 가능한 REST/gRPC 구조가 개발·디버깅에 더 효율적이라고 판단했다.

